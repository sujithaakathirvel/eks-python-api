apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eks-python-api
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: eks-python-api
                port:
                  number: 8000